import { SeriesofimageDirective } from './seriesofimage.directive';

describe('SeriesofimageDirective', () => {
  it('should create an instance', () => {
    const directive = new SeriesofimageDirective();
    expect(directive).toBeTruthy();
  });
});
